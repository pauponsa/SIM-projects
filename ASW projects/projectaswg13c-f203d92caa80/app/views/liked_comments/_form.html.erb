<%= form_with(model: liked_comment) do |form| %>
  <% if liked_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(liked_comment.errors.count, "error") %> prohibited this liked_comment from being saved:</h2>

      <ul>
        <% liked_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :comentari_id %>
    <%= form.text_field :comentari_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
